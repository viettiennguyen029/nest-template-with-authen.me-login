export enum BET_STATUS {
  INIT = 'INIT',
  CONFIRMED = 'CONFIRMED',
  WIN = 'WIN',
  LOSE = 'LOSE',
  REJECT = 'REJECT',
}

export enum ODDS_CODE {
  HOME = 'HOME',
  AWAY = 'AWAY',
  DRAW = 'DRAW',
}
